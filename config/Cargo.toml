[package]
name = "screenstub-config"
version = "0.0.1"

[dependencies]
serde = "^1.0.27"
serde_derive = "^1.0.27"
input-linux = { version = "^0.0.1", features = ["with-serde"] }

[features]
with-ddcutil = []
